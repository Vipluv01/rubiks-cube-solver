.venv/
__pycache__/
*.pyc

# results/corner_pdb.npy is intentionally tracked (not ignored): the
# deployed web demo needs it present at build time, and rebuilding it
# on a memory-constrained free-tier build machine risks an OOM kill.
# It's 88MB, under GitHub's 100MB hard limit.
